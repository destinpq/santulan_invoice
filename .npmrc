# Force npm to use node-gyp for building native modules
force=true

# Prefer offline cache
prefer-offline=true

# Disable SWC binary usage
next_no_swc=1

# Set architecture explicitly
target_arch=x64 